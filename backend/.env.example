PORT=4000
JWT_SECRET="your_jwt_secret_key"
MONGO_URI="" # Optional: MongoDB Atlas URI. If omitted, runs in resilient local JSON storage mode.
STRIPE_SECRET_KEY="" # Optional: Stripe Secret Key for online card payments. If omitted, simulated checkout mode is used.
FRONTEND_URL="http://localhost:5173"
